# webpack practice

## command

```
# development (include start server)
yarn start

# development (not start server)
yarn dev

# build (publish files to public directory)
yarn build

# add package
yarn -D <package>
```
